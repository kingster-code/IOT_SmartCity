@page "/submenu2"

@using IOT_Data.Models
@using Monitor_Business.interfaces
@using Monitor_Business.DTO
@inject IZoneBusiness funcoes
<div class="container-fluid">
	<div class="row">
            <h2 style="position:center;">
                Dados Atmosféricos
            </h2>
            <p>
                <img alt="Bootstrap Image Preview" style="
                
                display: block;
                margin-left: auto;
                margin-right: auto;
                width:70%" src="css/images/dadosatm.png" />
            </p>
            <p>
                Na secção dados atmosféricos é possivel tirarmos conlusões  relativamente aos gases poluentes de uma determinada zona.
                Deste modo, com o gráfico fornecido é possivel fazermos uma análise de dados ao longo dos anos.
            </p>
            <p>
                <a class="btn" href="dadosatm">View details »</a>
            </p>
    </div>
</div>
@code {
    private List<SensorDTO> zones = new List<SensorDTO>();

    protected override async Task OnInitializedAsync()
    {

        var rng = new Random();
        var test = new List<SensorDTO>();

        for (int i = 0; i < 8; i++)
        {
            test.Add(new SensorDTO
            {
                ZonaId = i,
                NumberSensor = rng.Next(5, 20)
            });
        }
        zones = test;


        //zones=funcoes.GetZoneNumberSensor();

    }
}
